#include <stdio.h>

// 出力変換指定子: 外部のデータを文字列に変換して表示したい時に、その変換方法を指定する記号。
// %d 指定子: 整数値を文字列に変換する。d は decimal (10進数) の略。
int main(void)
{
  // printf("%d", 100);
  // printf("円\n");
  printf("%d円\n", 100);
  return 0;
}
