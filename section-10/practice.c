/* 問題 1 */
// 先判定

/* 問題 2 */
// 後判定

/* 問題 3 */
// 利息1%の銀行に10000円を預けると何年で15000円を超えるかをシミュレーションするプログラム (複利の力)

/* 問題 4 */
#include <stdio.h>

int main(void)
{
  int score;

  do {
    printf("点数を入力してください: ");
    scanf("%d", &score);
  } while (score < 0 || score > 100);

  return 0;
}

/* 問題 5 */
// 必ず最初に処理を実行できるから